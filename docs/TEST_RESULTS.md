# Результаты проверки 19 июля 2026

## Чистая сборка

Команда `gradlew.bat clean build --no-daemon` для версии 0.1 завершилась `BUILD SUCCESSFUL` за 31 секунду. Выполнены clean, compileJava, processResources, reobfJar, jarJar, reobfJarJar, test, check и build.

Готовый мод: `build/libs/xrshop-0.1-all.jar`, размер 14 405 697 байт, SHA-256 `B187F92D73240E69FF2CB2C6194F1196F034D20260E79346C13195475321D633`. Проверено наличие вложенного `META-INF/jarjar/sqlite-jdbc-3.46.1.3.jar`, `V1__initial.sql`, `ru_ru.json`, обычного и расширенного редакторов, серверных команд и экрана магазина.

Файл `META-INF/mods.toml` прочитан непосредственно из итогового JAR как UTF-8. Русское описание отображается без mojibake: `Серверно-авторитетный внутриигровой магазин с валютой XR.`

## Автоматические тесты

JUnit: 70 выполнено, 0 failures, 0 errors, 0 skipped. Отчёт Gradle находится в `build/reports/tests/test/index.html`.

Покрыты: пустой/валидный/повреждённый магазин, неизвестная schema, неизвестные поля, дубликаты ID, цена, `${...}` placeholders и синтаксис команды; отсутствие конфликта placeholders с JSON/NBT-скобками; отклонение повреждённого `${...}`; успех команды по Brigadier callback, включая успешный результат с числом `0`, и отказ при отсутствующем/неуспешном callback; overflow, insufficient funds, конкурентное списание, идемпотентное начисление; блокировка повторной покупки, partial execution, EXECUTING recovery; revision/STORE_UPDATED, фильтрация DTO, точные Forge permission nodes и правило «главное либо дочернее административное право»; старый session ID и очистка клиентского Store/Editor DTO; editor save, base_revision conflict и backup recovery; физические разрешения 1280×720, 1920×1080, 2560×1440 с несколькими логическими GUI Scale; полное покрытие всех доступных полей `ShopConfig` схемой нового GUI; отсутствие нерелевантных свойств в оформлении товара; передача `grid_left_padding` и итогового `icon.size` через серверный DTO; наследование только размера иконки товара из категории при `icon.size: 0` с сохранением предмета, количества и NBT товара; валидация категорийного и собственного размеров; диапазоны новых полей; расчёт сетки с левым отступом; четыре варианта адаптивной компоновки иконки, заголовка, цены и кнопки без перекрытия; преобразование описания, команды, транзакции, прокрутка и текстурные режимы.

Последний dev-клиент Forge был запущен для внутренней версии 1.3.0: MinecraftForge 47.4.10 инициализирован, клиент дошёл до запуска OpenAL и Sound Engine. Публичная версия 0.1 дополнительно прошла чистую клиентскую компиляцию, reobfuscation и 70 автоматических тестов.

## Runtime smoke-test

Настоящий Forge dedicated server 1.20.1-47.4.10 запущен через `runServer`. Подтверждены:

- `Done (3.180s)` и `XR Shop запущен, revision=1`;
- инициализация Forge Permission API;
- создание пустых `shop.json`, `server.json`, `messages.json`;
- создание SQLite schema version 1 с таблицами balances, ledger, purchases, command_results, purchase_limits и audit, 11 индексами;
- регистрация и выполнение через RCON `/ms admin validate`, `/ms admin backup`, `/ms admin history`, `/ms admin review list`;
- штатный `stop`, сохранение всех dimensions, остановка RCON и `BUILD SUCCESSFUL`;
- отсутствие оставшихся Java-процессов.

После изменений 1.2.0 выполнен повторный запуск: сервер достиг `Done (3.505s)`, инициализировал Forge Permission API и записал `XR Shop запущен, revision=1`. В журнале нет ошибок XR Shop; после проверки тестовые процессы остановлены и порт 25565 освобождён.

Статический аудит не нашёл маркеров незавершённой работы, исключений неподдержанной реализации, пустых обработчиков, конкретных товаров или категорий в main-коде, а также файлового I/O в клиентском пакете.
